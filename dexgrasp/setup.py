from setuptools import setup, find_packages

setup(
    name='DexRep',
    version='1.0',
    description='',
    packages=find_packages(),
    install_requires=[
        # list of your package dependencies
    ],
    python_requires='>=3.8',
)